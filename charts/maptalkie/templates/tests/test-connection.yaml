apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maptalkie.fullname" . }}-test-connection"
  labels:
    {{- include "maptalkie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maptalkie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
